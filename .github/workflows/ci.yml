name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build Brisa
        run: cd Brisa && ./Scripts/build.sh
      - name: Run tests
        run: cd Brisa && ./Scripts/test.sh
